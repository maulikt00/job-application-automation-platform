"""Website connectors (GreenhouseConnector, LeverConnector,
WorkdayConnector, ...). Each connector implements the
WebsiteConnector interface for one job platform. Adding support for
a new site means adding a new connector file, not modifying existing
ones."""
