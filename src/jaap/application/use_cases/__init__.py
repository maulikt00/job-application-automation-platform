"""Use cases: one class per user-facing action the system supports
(e.g. GenerateCoverLetterUseCase, AutofillApplicationUseCase). Each
use case is composed with whatever interface implementations it
needs via constructor (dependency) injection.
"""
