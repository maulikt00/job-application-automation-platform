"""Application layer (use cases).

Contains the orchestration logic that expresses *what the software
does*, plus the abstract interfaces (ports) that use cases depend on
for persistence, AI, browser automation, and website connectors.
This layer depends only on the domain layer.
"""
