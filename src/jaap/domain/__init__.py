"""Domain layer.

Contains the core business objects and rules of the application
(Profile, Resume, JobPosting, Application, etc.) with zero dependencies
on databases, browsers, AI SDKs, or any other framework. Every other
layer depends on this one; this layer depends on nothing else in the
project.
"""
