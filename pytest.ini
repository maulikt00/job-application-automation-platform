[pytest]
minversion = 8.0
testpaths = tests
pythonpath = src
addopts = -ra --strict-markers
markers =
    unit: fast, isolated tests with no real I/O
    integration: tests that exercise real infrastructure (db, filesystem)
